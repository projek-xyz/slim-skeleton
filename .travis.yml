language: php

sudo: false

php:
  - 5.5
  - 5.6
  - 7.0
  - 7.1

cache:
  directories:
    - node_modules
    - vendor

env:
  global:
    - setup=basic
    - CI_BUILD_NUMBER="$TRAVIS_BUILD_NUMBER"
    - CI_PULL_REQUEST="$TRAVIS_PULL_REQUEST"
    - CI_BRANCH="$TRAVIS_BRANCH"

matrix:
  fast_finish: true
  include:
    - php: 5.5
      env: setup=resource
    - php: 5.5
      env: setup=coverage
  allow_failures:
    - php: 7.1

mysql:
  database: $DB_NAME
  username: $DB_USER
  encoding: utf8

before_install:
  - mysql -e "create database if not exists ${DB_NAME}"
  - if [[ $setup == 'resource' ]]; then $(curl -fsSL https://git.io/vP4hF | sh -) && source ~/.nvm/nvm.sh && nvm install 6; fi

install:
  - if [[ $setup == 'basic' ]]; then composer install -n -q; fi
  - if [[ $setup == 'coverage' ]]; then composer require codeclimate/php-test-reporter:~0.3 -n -q; fi
  - if [[ $setup == 'resource' ]]; then npm -g update -s && npm i -s; fi
  - if [[ $setup != 'resource' ]]; then php app/console migrate; fi

script:
  - if [[ $setup == 'basic' ]]; then composer lint -q && composer test -q; fi
  - if [[ $setup == 'coverage' ]]; then php vendor/bin/phpunit -c tests/phpunit.xml --coverage-clover build/logs/clover.xml; fi
  - if [[ $setup == 'resource' ]]; then gulp build; fi

after_script:
  - if [[ $setup == 'coverage' ]]; then php vendor/bin/test-reporter &&  php vendor/bin/coveralls -v; fi

notifications:
  email: false
